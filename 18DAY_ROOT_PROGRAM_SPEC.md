# 18day_root 프로그램 구조·특성·재구축 명세

## 1. 프로그램 정체성

`18day_root`는 어원 기반 영어 단어를 **한 번의 진입 안에서 테스트 → 즉시 암기 → 오답 반복 → 관리자 보고**까지 완료하는 단일 흐름형 웹앱이다. 학습자가 테스트 메뉴, 오답 메뉴, 복습 메뉴를 따로 찾아다니지 않도록 모든 활동을 하나의 상태 흐름으로 연결한다.

핵심 목표는 다음과 같다.

1. 어원을 먼저 보되 정답 뜻을 미리 노출하지 않는다.
2. 선택형을 우선하여 태블릿·스마트폰의 피로를 낮춘다.
3. 틀린 단어만 철자 타이핑으로 각인한다.
4. 숙달된 단어는 설명·대기 단계를 단축한다.
5. DAY 마지막에는 학습자가 관리자 완료 보고를 직접 보내며 오늘 학습을 복기한다.
6. 보고 메일 전송 성공 전에는 DAY를 완료로 기록하지 않는다.

## 2. 콘텐츠 및 일정 구조

- ROOT·접두사·접미사 단위: **360개**
- 표제어: **1410개**
- 과정: **18DAY**
- DAY당 단어: **74~82개**, 평균 **78.3개**
- DAY당 집중블록: **4개**
- 전체 집중블록: **72개**
- 분할 원칙: 원교재 순서 유지 → ROOT 학습단위 유지 → 원교재 DAY 경계 우선 → 분량 편차 최소화

## 3. 단일 학습 루프

### 3.1 ROOT 이해

ROOT와 핵심 의미를 크게 표시한다. 해당 ROOT가 전부 숙달된 경우 설명 단계를 짧게 처리한다.

### 3.2 어원 나타내기

단어 → 구성 요소 → 어원 흐름 순서로 단계적으로 나타난다. 구성 요소와 핵심 어근은 굵게·강조색으로 제시한다. 최종 한국어 정답은 선택 전 숨긴다.

### 3.3 선택형 테스트

4지선다로 뜻을 고른다. 입력 방식은 키보드 숫자 1~4, 숫자패드, 마우스, 터치를 모두 지원한다.

### 3.4 즉시 오답 각인

오답이면 같은 화면에서 정답을 확인하고 **오늘 오답으로 등록됨**을 명확하게 표시한다. 블록이 끝나면 별도 메뉴 이동 없이 자동으로 철자 회상 단계가 이어진다.

### 3.5 누적 반복

오답 철자는 기본 2회 성공할 때까지 누적된다. 당일 오답과 이전 누적 오답을 서로 다른 배지로 구분한다. 한 블록에 너무 많은 누적 오답이 몰리지 않도록 최대 12개까지 포함한다.

### 3.6 블록 보상

각 블록 완료 직후 정답·오답·철자 회상 횟수를 보여준다. 그 다음 블록으로 바로 이동한다.

### 3.7 관리자 메일 완료 게이트

4개 블록이 끝나면 큰 전체화면 팝업이 나타난다. ROOT 수, 단어 수, 오늘 오답, 남은 철자 반복을 복기한 뒤 **관리자에게 완료 보고 보내기**를 눌러야 한다. 메일 전송이 성공해야 `completedAt`과 완료 DAY가 저장된다. 실패하면 DAY는 미완료로 유지된다.

## 4. 상태 구조

브라우저 저장키: `18dayRootStateV5`

주요 상태:

- `currentDay`: 현재 DAY
- `completedDays`: 완료 DAY 목록
- `days[day]`: 블록, 단어 위치, 정오답, 시간, 완료·메일 상태
- `mastery[wordId]`: 단어별 숙달 점수와 정오답 누계
- `spellingNotebook[wordId]`: 철자 반복 잔여 횟수
- `pendingReports`: 전송 실패 보고서
- `sentReportIds`: 중복 메일 방지 ID

이전 24DAY·18DAY 기록은 단어 ID를 기준으로 새 18DAY 일정에 재배치한다. DAY 번호를 단순 환산하지 않는다.

## 5. 파일 구조

```text
index.html                         화면·메일 완료 팝업
styles.css                        반응형 UI·강조·터치 영역
app.js                            학습 상태기계·오답·숙달·메일 게이트
data/voca_schedule.json           18DAY·72블록 일정
data/learning_units.json          ROOT와 표제어 학습 데이터
data/root_units.json              ROOT 기준 데이터
netlify/functions/send-day-report.mjs  관리자 메일 서버리스 함수
netlify.toml                      Netlify 배포·보안 헤더
```

## 6. 다른 교재로 동일 프로그램을 만드는 방법

### 필수 데이터 필드

ROOT 단위:

- `id`
- `root`
- `root_meaning`
- `source_day`
- `words[]`

단어:

- `id`
- `word`
- `quiz_meaning`
- `etymology_formula[]`
- `etymology_steps[]`
- `source_page`
- `importance`

### 재구축 절차

1. 새 교재에서 ROOT와 표제어를 순서대로 추출한다.
2. ROOT 단위와 표제어 ID를 고정한다.
3. 원하는 DAY 수를 정한다.
4. ROOT를 자르지 않는 연속 구간 분할로 DAY를 만든다.
5. 각 DAY를 4개 블록으로 다시 균형 분할한다.
6. 학습 데이터 JSON만 교체하고 동일한 학습 엔진을 재사용한다.
7. 어원 흐름 마지막 단계에 정답 뜻이 포함되면 선택 전 제거한다.
8. 모바일·태블릿·PC와 메일 성공 게이트를 회귀 검사한다.

## 7. 시인성·단순성 원칙

- 홈에는 오늘 DAY, 시간, 분량, 남은 시간, 남은 단어, 4개 블록만 표시한다.
- 학습 화면에는 현재 문제에 필요한 정보만 표시한다.
- 주요 단어는 반응형 큰 글자와 높은 대비를 사용한다.
- 터치 목표는 최소 48px, 선택지는 80px 이상을 확보한다.
- 스마트폰에서는 1열, 태블릿·PC에서는 2열 선택지를 사용한다.
- 긴 설명, 중복 안내, 별도 메뉴 이동은 피한다.

## 8. 호환성 기준

- PC: 1366×768 이상, 키보드 숫자·마우스
- 태블릿: 768px 전후, 터치·세로/가로
- 스마트폰: 320~430px, 안전영역·한 손 터치
- 브라우저: 최신 Chrome, Edge, Safari
- 저장: 단일 기기 `localStorage`; 다기기 동기화는 DB 연결 필요
- 메일: GitHub에 소스를 보관하되 실제 운영 배포는 Netlify 권장

## 9. 운영상 중요 제한

GitHub Pages만으로는 Netlify Functions가 실행되지 않는다. **메일 성공을 DAY 완료 조건으로 사용하는 운영본은 Netlify 배포와 `RESEND_API_KEY`, `REPORT_FROM_EMAIL` 설정이 필수**다. 메일 설정이 없으면 학습자는 완료 팝업에서 실패 메시지를 확인하고 DAY는 미완료로 남는다. 관리자는 강제 완료로 예외 처리할 수 있다.
